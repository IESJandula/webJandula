---
// src/components/CardNoticia.astro
//
// Envoltorio fino: el marcado esta en src/lib/cardNoticia.ts porque el
// navegador tiene que poder pintar la misma tarjeta al refrescar las noticias
// sin reconstruir la web.
import { cardNoticiaHTML, type NoticiaCardProps } from '@/lib/cardNoticia';

const props = Astro.props as NoticiaCardProps;
---

<Fragment set:html={cardNoticiaHTML(props)} />
